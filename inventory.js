import { supabase, formatCurrency, showToast } from './supabase.js'  export const initInventory = async (storeId) => {   const main = document.getElementById('main-content')   main.innerHTML = '<div class=skeleton-loader>Carregando estoque...</div>'   const { data } = await supabase.from('stock_summary').select('*').eq('store_id', storeId)   main.innerHTML = `     <h1>Estoque</h1>     <div class=card style=margin-top:1.5rem>       <div class=table-container>         <table>           <thead>             <tr><th>Produto</th><th>Codigo</th><th>Qtd</th><th>Unidade</th><th>Custo Total</th></tr>           </thead>           <tbody>             ${data?.map(item => `               <tr>                 <td>${item.product_name}</td>                 <td>${item.code || '-'}</td>                 <td>${item.total_quantity}</td>                 <td>${item.base_unit}</td>                 <td>${formatCurrency(item.total_cost)}</td>               </tr>             `).join('') || '<tr><td colspan=5 style=text-align:center>Nenhum item em estoque</td></tr>'}           </tbody>         </table>       </div>     </div>   ` }